import React from "react";
import ScenarioList from "../components/ScenarioList";

const Scenarios = ({ onAddExcuse }) => {
  return <ScenarioList onAddExcuse={onAddExcuse} />;
};

export default Scenarios;
